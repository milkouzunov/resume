
import ReactLoading from 'react-loading';

const Loader = () => (
    <ReactLoading className="loader-spinner" type="spinningBubbles" color="#179038" height={100} width={100} />
);

export default Loader;
 
